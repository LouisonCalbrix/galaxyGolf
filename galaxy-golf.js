// A golfing game taking place in outer space
// date: October 2020
// author: Louison Calbrix

let Ball = function(pos) {
    // attributes: position, velocity, hitbox
    // methods: update
}

let Level = function(posStart, posGoal) {
    // attributes: goal, ball
    // goal = {x: xGoal, y: yGoal}
    // methods: update
}
